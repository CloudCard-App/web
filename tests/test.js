var assert = require('chai').assert;

describe("Testing for funz", function () {
  describe("More funz", function () {
    it("tests for equality", function () {
      assert.equal(2, 2);
    });
  });
});